# define what ever (vairables or files) you are reading only onces, and dont change

from pathlib import Path


CONFIG_FILE_PATH = Path("config/config.yaml") #config/config.yaml
PARAMS_FILE_PATH = Path("params.yaml")
